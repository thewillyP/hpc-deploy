# Cluster facts only. Anything derivable is derived in bin/deploy.
# ACCOUNT intentionally empty -- greene jobs do not pass --account.
ACCOUNT=
LOGIN_HOST=greene
SCRATCH=/scratch/wlp9800
OVERLAY_SRC=/scratch/work/public/overlay-fs-ext3/overlay-50G-10M.ext3.gz

# greene puts logs on /vast, not /scratch
LOG_DIR=/vast/wlp9800/logs